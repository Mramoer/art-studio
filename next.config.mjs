/** @type {import('next').NextConfig} */
const nextConfig = {
	images: {
		domains: [
			'lh3.googleusercontent.com',
			's.gravatar.com',
			'res.cloudinary.com',
		],
	},
};

export default nextConfig;
